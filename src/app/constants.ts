export const BUTTONS_PARENT = document.getElementById('buttons') as HTMLDivElement;

export enum LinkAttribute {
  weight = 'weight',
  bias = 'bias',
  learningRate = 'learningRate',
  activation = 'activation',
  regularization = 'regularization',
  regularizationRate = 'regularizationRate',
}
